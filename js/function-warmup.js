    function makeUsername(firstName, age) {
        return (firstName.length) + (firstName.charAt(0) .toLowerCase() + age);
    }


   var username = makeUsername('John', 20);
 console.log('John', 20, username);


    function makeUsername(firstName, age) {
        return (firstName.length) + (firstName.charAt(0) .toLowerCase() + age);
    }

      var username = makeUsername('Sandy', 25);
    console.log('Sandy', 25, username);